# Pinned: DLLs compiled with rustc 1.97.x (1.97.0 and 1.97.1) fail to apply
# SpEffects (phantom skins log "Applied" but have no in-game effect); 1.96.1
# is the last known-good compiler. Bump only after verifying phantom skins
# in-game, and keep the dtolnay/rust-toolchain pin in
# .github/workflows/build-mod*.yml in sync.
[toolchain]
channel = "1.96.1"
